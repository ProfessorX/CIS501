% 20131117 Lab 23:00
% Always be self-motivating.

%% Implementation
ent = zeros(1, 11);
for count = 0:10
    ent(count+1) = Entropy([repmat(1, 1, count), repmat(0, 1, 10-count)])
end


%% Plotting
plot(0:10, ent);
xlabel('Number of 1s');
ylabel('ENtropy')